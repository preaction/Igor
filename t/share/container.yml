success:
    $class: Local::Runnable
    exit: 0

fail:
    $class: Local::Runnable
    exit: 1

nonrunnable:
    $class: Moo

unloadable:
    $class: DOES::NOT::EXIST

hidden_array:
    - $class: Local::Runnable
      why: Arrays are not addressable from Igor->get()

hidden_hash:
    hidden_by:
        $class: Local::Runnable
        why: Hashes are not addressable from Igor->get()

extends:
    $extends: success

alias:
    $ref: success

